{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {removeApplication = id; toggleSelectedWindowStickyState = id; }; 
            CLASS = VTApplicationViewController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                mApplicationsController = NSArrayController; 
                mWindowsController = NSArrayController; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}